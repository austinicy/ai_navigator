# Anthropic API key — required for live chat, document signal extraction, and roadmap generation.
# Get one at https://console.anthropic.com/
# Without it, the app runs but Claude-dependent features degrade gracefully (see README).
ANTHROPIC_API_KEY=
